# nodejs-ts-handlebars
Ejemplo practico integrando nodejs con typescript y handlebars

antes de ejecutar el server, primero ejecutar el comando npm install o npm i para descargar la carpeta node_modules
